<div class="moduk-warning-text {{- ' ' + params.classes if params.classes}}"
  {%- for attribute, value in params.attributes %} {{attribute}}="{{value}}"{% endfor -%}
  >
  <span class="moduk-warning-text__icon" aria-hidden="true">!</span>
  <strong class="moduk-warning-text__text">
    <span class="moduk-warning-text__assistive">{{ params.iconFallbackText }}</span>
    {{ params.html | safe if params.html else params.text }}
  </strong>
</div>
