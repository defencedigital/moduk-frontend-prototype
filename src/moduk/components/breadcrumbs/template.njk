{# Set classes for this component #}
{%- set classNames = "moduk-breadcrumbs" -%}

{% if params.classes %}
  {% set classNames = classNames + " " + params.classes %}
{% endif -%}

{% if params.collapseOnMobile %}
  {% set classNames = classNames + " moduk-breadcrumbs--collapse-on-mobile" %}
{% endif -%}

<div class="{{classNames}}"{% for attribute, value in params.attributes %} {{attribute}}="{{value}}"{% endfor %}>
  <ol class="moduk-breadcrumbs__list">
  {% for item in params.items %}
    {% if item.href %}
    <li class="moduk-breadcrumbs__list-item">
      <a class="moduk-breadcrumbs__link" href="{{ item.href }}"{% for attribute, value in item.attributes %} {{attribute}}="{{value}}"{% endfor %}>{{ item.html | safe if item.html else item.text }}</a>
    </li>
    {% else %}
    <li class="moduk-breadcrumbs__list-item" aria-current="page">{{ item.html | safe if item.html else item.text }}</li>
    {% endif %}
  {% endfor %}
  </ol>
</div>
