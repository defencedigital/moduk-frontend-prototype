{% set headingLevel = params.headingLevel if params.headingLevel else 3 %}
<div class="moduk-warning-callout
{%- if params.classes %} {{ params.classes }}{% endif %}"
{%- for attribute, value in params.attributes %} {{attribute}}="{{value}}"{% endfor %}>
  <h{{ headingLevel }} class="moduk-warning-callout__label">
    {%- if "Important" in params.heading or "important" in params.heading %}
    {{ params.heading | safe }}<span class="moduk-u-visually-hidden">:</span>
    {%- else %}
    <span role="text">
      <span class="moduk-u-visually-hidden">Important: </span>
      {{ params.heading | safe }}
    </span>
    {%- endif %}
  </h{{ headingLevel }}>
  {{ params.HTML | safe }}
</div>
