<figure class="moduk-image
{%- if params.classes %} {{ params.classes }}{% endif %}"
{%- for attribute, value in params.attributes %} {{attribute}}="{{value}}"{% endfor %}>
  <img class="moduk-image__img" src="{{ params.src }}" alt="{{ params.alt }}"
  {%- if params.sizes and params.srcset %}
    sizes="{{ params.sizes }}" srcset="{{ params.srcset }}"
  {%- endif -%}
  >
  {%- if params.caption %}
  <figcaption class="moduk-image__caption">
    {{ params.caption | safe }}
  </figcaption>
  {%- endif %}
</figure>
